apiVersion: v1
kind: Service
metadata:
  name: microfront-hpa
  namespace: microfront
  labels:
    app: microfront-hpa
spec:
  type: ClusterIP
  selector:
    app: microfront-hpa
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
